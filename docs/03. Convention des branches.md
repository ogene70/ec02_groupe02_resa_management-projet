# 🌳 Convention de Nommage des Branches Git

Pour garantir une gestion propre du code source et faciliter la collaboration, nous adoptons une convention stricte pour le nommage des branches.

## 🏗️ Structure Globale

Chaque nom de branche doit suivre ce format précis :

`type/contexte/description-courte`

- **`type`** : La nature de la modification (voir liste ci-dessous).
- **`contexte`** : (Optionnel mais recommandé) Le scope touché (`front`, `back`, `api`, `auth`, etc.) ou le numéro du ticket Jira/Trello (ex: `JIRA-123`).
- **`description-courte`** : Un résumé en 2-3 mots, en **kebab-case** (minuscules, séparées par des tirets).

## 🏷️ Les Types Autorisés

| Type           | Description                                                   | Exemples                           |
|----------------|---------------------------------------------------------------|------------------------------------|
| **`feat`**     | Nouvelle fonctionnalité ou évolution majeure.                 | `feature/front/login-page`         |
| **`fix`**      | Correction de bug.                                            | `fix/back/navbar-responsiveness`   |
| **`hotfix`**   | Correction urgente en production (contourne le cycle normal). | `hotfix/api/security-patch`        |
| **`docs`**     | Modification de la documentation uniquement.                  | `docs/readme-update`               |
| **`chore`**    | Maintenance technique (config, build) sans impact métier.     | `chore/docker/update-node-version` |
| **`refactor`** | Réécriture de code sans changement de fonctionnalité.         | `refactor/front/auth-service`      |
| **`test`**     | Ajout ou modification de tests.                               | `test/e2e/login-flow`              |
| **`cicd`**     | Maintenance GitHub, Docker, déploiement (compose, CI/CD).     | `cicd/docker/compose`              |
| **`style`**    | Amélioration du style.                                        | `style/login/forms`                |

## 🚦 Le Workflow (Git Flow Simplifié)

### 1. Branches Principales (Protégées)

Ces branches existent en permanence. **Interdiction de pousser directement dessus** (Merge Request obligatoire).

- `master` : Code de **production**. Stable, testé, déployable à tout moment.
- **`develop`**: Code de **développement**. C'est ici que toutes les fonctionnalités sont fusionnées avant la release.

### 2. Cycle de vie d'une fonctionnalité

1. **Création** : Je pars de `develop` pour créer ma branche.

    ```
    git checkout develop
    git pull origin develop
    git checkout -b feature/front/ma-description

    ```

2. **Développement** : Je fais mes commits (en respectant la convention de commits !).
3. **Fusion** : Je crée une **Pull Request (PR)** ou **Merge Request (MR)** vers `develop`.
    - _Titre de la PR_ : `feat(scope): Description claire`
    - _Revue_ : Un collègue doit valider le code.
4. **Suppression** : Une fois la PR fusionnée dans develop, la branche `feature/...` est supprimée.

## ✅ Exemples Concrets

### ✅ Bons Noms

- `feature/front/user-profile`
- `fix/ticket-42/login-button`
- `chore/docker/upgrade-postgres`
- `docs/api/add-endpoints`

### ❌ Mauvais Noms (À éviter)

- `lucas-dev` (Ne dit pas ce que ça fait)
- `mise-a-jour` (Trop vague)
- `fix_bug` (Mauvais format, pas de type clair)
- `Feature/AuthService` (Pas de majuscules !)

## 🛠️ Astuce Configuration (Hooks)

Pour empêcher physiquement les développeurs de commiter sur une branche mal nommée, nous utilisons un **Hook Git**.

### 1. Installation de Husky (à la racine)

```
npm install --save-dev husky
npx husky init

```

### 2. Création du Hook `pre-commit`

Création du fichier `.husky/pre-commit` pour vérifier le nom de la branche avant chaque commit.

```jsx
#!/bin/sh

echo "------------------------------------------------"
echo "🤖 HUSKY : Vérification du nom de la branche..."
echo "------------------------------------------------"

# Récupérer le nom de la branche
branch_name=$(git rev-parse --abbrev-ref HEAD)

echo "Branche actuelle : '$branch_name'"

# Liste des branches ignorées
if [ "$branch_name" = "master" ] || [ "$branch_name" = "main" ] || [ "$branch_name" = "develop" ]; then
echo "✅ Branche protégée/principale. Vérification ignorée."
exit 0
fi

# Regex de validation
valid_branch_regex="^(feat|fix|hotfix|docs|chore|style|refactor|test|cicd)\/[a-z0-9-]+(\/[a-z0-9-]+)?$"

if ! echo "$branch_name" | grep -Eq "$valid_branch_regex"; then
echo "❌ ERREUR : Nom de branche invalide !"
echo "------------------------------------------------"
echo "Le nom '$branch_name' ne respecte pas la convention."
echo "Format attendu : type/contexte/description-courte"
echo "Exemples valides :"
echo "  - feat/front/login-page"
echo "  - fix/api/user-controller"
echo "  - chore/docker/setup"
echo "------------------------------------------------"
echo "🚫 COMMIT BLOQUÉ."
exit 1
fi

echo "✅ Nom de branche valide. Commit autorisé."
exit 0
```

### 3. Résultat

Si un développeur essaie de commiter sur une branche nommée `toto-test`, le commit sera **refusé** avec un message d'erreur explicite.
